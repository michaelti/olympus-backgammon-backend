{
  "name": "olympus-backgammon-backend",
  "version": "0.1.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon --watch src/ --exec 'npm run lint && npm run start-server'",
    "start-server": "node -r dotenv/config src/index.js",
    "lint": "eslint src/; exit 0"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "prompt-sync": "^4.2.0",
    "ramda.clone": "^0.26.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "eslint": "^7.2.0",
    "nodemon": "^2.0.4"
  },
  "eslintConfig": {
    "env": {
        "commonjs": true,
        "es2020": true,
        "node": true
    },
    "extends": "eslint:recommended",
    "parserOptions": {
        "ecmaVersion": 11
    },
    "rules": {}
  },
  "nodemonConfig": {
    "events": {
      "start": "clear"
    }
  }
}
